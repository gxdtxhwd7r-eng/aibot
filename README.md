# АМК AI — Комиссионная продажа

Локальный помощник менеджера автосалона: ручной режим анализирует один текст, а массовый режим импортирует дневную XLSX-выгрузку CM.Expert, исключает дубли и готовит сообщения. Приложение не отправляет сообщения автоматически.

## Что уже работает

- Определение автомобиля, подходящего «крючка» и генерация живого обращения от Никиты, компания АМК.
- Структурированный ответ API (`ok` или `do_not_contact`).
- Предварительная локальная проверка прямых юридических обременений и запретов на обращения автосалонов — до отправки текста в OpenAI.
- Особая обработка фразы «перекупов не беспокоить»: она сама по себе не блокирует предложение честной комиссионной продажи.
- Понятные ошибки при пустом поле, отсутствующем ключе и недоступном API.
- Копирование результата, повторная генерация и очистка формы.
- Отдельная база сценариев возражений для будущего этапа.
- Импорт `.xlsx` по нормализованным названиям колонок, без зависимости от их порядка.
- Локальная SQLite-база лидов, дедупликация и safety-проверка до OpenAI.
- Ограничение AI-обработки тремя одновременными запросами и повторное использование одного клиента на batch.
- Карточки лидов, polling прогресса и статусы менеджера «Связались»/«Пропустить».

## Массовая обработка CM.Expert

1. Откройте вкладку **«Массовая обработка CM.Expert»**.
2. Перетащите дневную выгрузку `.xlsx` и нажмите **«Загрузить и обработать»**.
3. Backend читает только значения ячеек через `openpyxl` (`data_only`, read-only), сопоставляя колонки по aliases в `backend/cm_expert_parser.py`. Макросы и содержимое ячеек не исполняются.
4. Новые строки записываются в SQLite. Прямые запреты отмечаются `do_not_contact`, пустые строки — `error`, а остальные ограниченно анализируются существующим `AIService`.
5. Интерфейс опрашивает `/api/leads`, пока обработка не завершится. Повторный импорт того же объявления вернёт его как duplicate и не вызовет OpenAI.

Основной ключ уникальности — ссылка/ID CM.Expert, затем нормализованная исходная ссылка, затем стабильный hash характеристик. Исходный XLSX обрабатывается в памяти и не сохраняется на диск.

## Быстрый запуск на Windows

### 1. Установите Python

Установите **Python 3.11 или новее** с [python.org](https://www.python.org/downloads/windows/). В установщике обязательно отметьте **Add Python to PATH**.

### 2. Установите проект

1. Скачайте или распакуйте папку проекта.
2. Дважды щёлкните `install.bat`.
3. Дождитесь сообщения об успешной установке.

Скрипт создаст изолированное окружение `.venv`, установит зависимости и скопирует `.env.example` в `.env` (если файла ещё нет).

### 3. Добавьте API-ключ

Откройте файл `.env` обычным Блокнотом и вставьте ключ после знака `=`:

```env
OPENAI_API_KEY=sk-ваш_ключ
OPENAI_MODEL=gpt-5.4-mini
```

Не добавляйте кавычки и не публикуйте файл `.env`. Он уже исключён из Git. Ключ используется только backend-сервером и никогда не попадает во frontend.

### 4. Запустите приложение

Дважды щёлкните `run.bat`. Откроется браузер по адресу:

```text
http://127.0.0.1:8000
```

Не закрывайте окно командной строки, пока пользуетесь приложением. Для остановки нажмите `Ctrl+C` или закройте окно.

## Установка командами (Windows PowerShell)

Если удобнее выполнять команды вручную:

```powershell
cd C:\путь\к\aibot
py -3.11 -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
pip install -r requirements.txt
Copy-Item .env.example .env
notepad .env
python -m uvicorn backend.main:app --host 127.0.0.1 --port 8000
```

Если PowerShell запрещает активацию скрипта, она не обязательна — используйте исполняемый файл окружения напрямую:

```powershell
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe -m uvicorn backend.main:app --host 127.0.0.1 --port 8000
```

## Запуск backend и frontend

Frontend — статическая страница, которую раздаёт backend. Поэтому для обычной работы нужен только **один** процесс:

```powershell
python -m uvicorn backend.main:app --reload
```

- Приложение: <http://127.0.0.1:8000>
- Проверка сервера: <http://127.0.0.1:8000/api/health>
- Интерактивная документация API: <http://127.0.0.1:8000/api/docs>

Отдельный Node.js-сервер и сборка frontend не нужны.

## Настройки `.env`

| Переменная | По умолчанию | Назначение |
|---|---:|---|
| `OPENAI_API_KEY` | пусто | Секретный ключ OpenAI (обязателен для генерации) |
| `OPENAI_MODEL` | `gpt-5.4-mini` | Модель генерации |
| `OPENAI_TIMEOUT_SECONDS` | `45` | Тайм-аут запроса к OpenAI |
| `APP_HOST` | `127.0.0.1` | Адрес для ручного запуска |
| `APP_PORT` | `8000` | Порт для ручного запуска |
| `DATABASE_PATH` | `data/amk_ai.sqlite3` | Путь к локальной SQLite-базе |
| `MAX_UPLOAD_MB` | `15` | Максимальный размер XLSX |
| `AI_CONCURRENCY` | `3` | Одновременные AI-запросы batch |

После изменения `.env` перезапустите сервер. Значения `APP_HOST` и `APP_PORT` документируют настройки; для изменения адреса при ручном запуске передайте их параметрами `uvicorn`.

## API

`POST /api/generate` — существующий одиночный анализ.

Запрос:

```json
{
  "advertisement": "Lada Vesta, 2021 год, один владелец, обслуживание у дилера"
}
```

Новые endpoints:

- `POST /api/import` — multipart upload XLSX и статистика (`total_rows`, `created`, `duplicates`, `blocked`, `queued`, `errors`);
- `GET /api/leads` и `GET /api/leads/{id}` — лиды и прогресс по статусам;
- `PATCH /api/leads/{id}/status` — статус менеджера `new`, `contacted` или `skipped`.

Успешный результат:

```json
{
  "status": "ok",
  "car": "Lada Vesta",
  "hook": "Один владелец и подтверждённое дилерское обслуживание",
  "message": "Здравствуйте! Никита, компания АМК...",
  "reason": null
}
```

При прямом запрете или обременении:

```json
{
  "status": "do_not_contact",
  "car": null,
  "hook": null,
  "message": null,
  "reason": "НЕ ПИСАТЬ — обнаружено юридическое обременение."
}
```

## Проверка проекта

Для разработки установите тестовые зависимости и запустите тесты:

```powershell
pip install -r requirements-dev.txt
pytest -q
```

Локальный тест health endpoint без расхода средств:

```powershell
Invoke-RestMethod http://127.0.0.1:8000/api/health
```

Автоматические тесты не выполняют реальные OpenAI-запросы: AI подменяется тестовой реализацией. Только ручная проверка успешной генерации через работающий сервер требует ключ и расходует API-квоту.

## Архитектура

```text
frontend/
  index.html          интерфейс
  styles.css          оформление
  app.js              API-клиент браузера
backend/
  main.py             FastAPI и маршруты
  ai_service.py       генерация структурированного ответа
  api_client.py       серверный клиент OpenAI
  config.py           переменные окружения
  system_prompt.py    правила AI
  knowledge_base.py   проверенные условия АМК
  safety.py           локальные стоп-проверки
  objections.py       сценарии возражений для следующего этапа
  models.py           модели запросов и ответов
  cm_expert_parser.py нормализация заголовков и безопасный XLSX parser
  database.py         SQLite repository и схема лидов
  batch.py            safety и ограниченная параллельная AI-обработка
tests/                автоматические тесты
```

## Новые зависимости

- `openpyxl` — чтение XLSX без запуска Excel/макросов;
- `python-multipart` — безопасный приём `UploadFile` в FastAPI.

Они устанавливаются обычным `pip install -r requirements.txt`; тестовые зависимости — из `requirements-dev.txt`.

## Приватность и ограничения

- Импортированные объявления и результаты записываются локально в `data/amk_ai.sqlite3`; API-ключ там не хранится. Каталог `data/`, XLSX и SQLite исключены из Git.
- API-ключ хранится только локально в `.env` и читается backend-сервером.
- В MVP **нет** интеграций с Авито, Дромом или Auto.ru.
- В MVP **нет** сбора объявлений, обхода CAPTCHA/антибот-систем и автоматической рассылки.
- Модуль ответа на возражения пока представлен только базой сценариев и не доступен в интерфейсе.
- Перед отправкой готового сообщения менеджеру следует проверить его на соответствие исходному объявлению.
- Аутентификации и разграничения менеджеров пока нет; repository/API изолированы так, чтобы добавить auth-слой следующим этапом. При публикации по публичному IP доступ следует ограничить reverse proxy/firewall.
- Обработка живёт в процессе FastAPI: отдельной надёжной очереди задач и автоматического retry после перезапуска пока нет.

## Частые проблемы

**«Не настроен OpenAI API»** — убедитесь, что файл называется именно `.env`, ключ указан как `OPENAI_API_KEY=...`, затем перезапустите `run.bat`.

**«Не удалось получить ответ от AI»** — проверьте интернет, корректность ключа, доступность выбранной модели и наличие API-квоты.

**Порт 8000 занят** — закройте другое приложение на этом порту или запустите сервер вручную с другим портом, например `python -m uvicorn backend.main:app --port 8001`, и откройте `http://127.0.0.1:8001`.
